#!/bin/sh
set -eu

RESEARCH_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
UV=/opt/homebrew/bin/uv

if [ ! -x "$UV" ]; then
  echo "medrec-research: /opt/homebrew/bin/uv is required" >&2
  exit 1
fi

exec rtk proxy "$UV" run --project "$RESEARCH_ROOT" \
  medrec "$@" --root "$RESEARCH_ROOT"
