# Separate the active research home from the archive

`medrec-research` is the sole Active Research Home, while `New-Search` remains a read-only Research Archive at its existing path and Git history. Copying the historical tree would preserve its mixed ownership and workflow noise; migrating only curated Research Memory preserves evidence while allowing a general MedRec architecture.
