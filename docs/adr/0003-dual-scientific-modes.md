# Separate reproduction from comparison

Baselines support Reproduction Mode for recorded upstream semantics and may separately qualify for Comparison Mode under the Unified Research Protocol. Forcing upstream methods into shared semantics would destroy reproduction evidence, while comparing unaligned results would make relative claims invalid; the Prediction Adapter translates representation while the Baseline Core stays frozen.
