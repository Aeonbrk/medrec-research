# Use the Mac as harness and 319 as execution plane

The MacBook Air owns synthetic protocol checks, submission, monitoring, and public-safe audit; 319 owns real EHR processing, training, GPU inference, and restricted artifacts. This split keeps the lightweight harness reproducible while preventing local resource limits and data copies from changing experimental semantics.
