# Historical benchmark-harness review

## Source review

- Review run: `20260711-104521-medrec`
- Reviewed baseline commit: `34e4d31`
- Review-fix commit: `2625b7d7cc657942a27f59c0aa3b0741dc96d746`
- Review verdict: `Not ready`
- Tracker result: no configured remote or durable issue tracker; these findings are recorded here instead.

The [Benchmark Authority Contract Hardening plan](../plans/2026-07-12-001-fix-authority-contract-hardening-plan.md) addressed these findings on `2026-07-12`. The entries below preserve the original review record; they are not a current backlog.

## Historical findings

- P1 `src/medrec_research/benchmark_program.py:653` — Variance policy accepts an incomplete output set. Bind a versioned expected output-ID set into the stability policy and require exactly one complete check per output.
- P1 `src/medrec_research/cli.py:394` — Revoked authority bundle remains usable until restart. Reload and strictly validate the bundle for action-context and POST evaluation, failing closed on missing or invalid state.
- P1 `src/medrec_research/harness.py:97` — Authority rotation breaks Web and CLI parity. Define one shared deterministic resolver or bundle invariant for Web, CLI, and future ARIS consumers. Independent validation was degraded by a platform `429`; agent-native review mechanically reproduced the mismatch.
- P1 `src/medrec_research/project_status.py:882` — Rotated audit reviews do not invalidate selection. Require the current review set at status publication and revalidate or rederive selection against current program, audits, and reviews.
- P1 `src/medrec_research/project_status.py:923` — Characterization starts without steward acceptance. Add a content-addressed acceptance authority that binds the selection, candidate, accepted decision, reviewer, and issue time.
- P1 `src/medrec_research/baseline_audit.py:601` — Conflicting pass and fail reviews still pass hard gates. Fail closed on conflicting decisions for the same candidate, audit digest, and claim.
- P1 `src/medrec_research/project_status.py:908` — Discovery bypasses a blocked current selection. Treat a blocked selection as a fail-closed invariant before review or discovery projection.
- P2 `src/medrec_research/baseline_audit.py:168` — Mutable URLs pass the immutable evidence gate. Validate host-specific immutable URL shapes and require the revision in the effective ref segment.
- P2 `src/medrec_research/baseline_audit.py:381` — Branched lineage cycles evade validation. Detect cycles over the complete adjacency graph for each lineage layer.
- P2 `src/medrec_research/baseline_audit.py:618` — Review-set digest depends on incidental record order. Canonicalize unique review records before serialization and digest calculation.
- P2 `src/medrec_research/web/app.js:386` — Invalid JSON is mislabeled as transport failure. Map JSON parse failures to the malformed state and preserve non-success HTTP responses as transport failures.
- P2 `src/medrec_research/web/app.js:424` — Disabled submit button cannot retain focus. Keep the control focusable with `aria-disabled` during submission and use the existing in-flight guard to reject repeated activation.
- P2 `src/medrec_research/action_gate.py:644` — Fail-closed intent digest branches lack tests. Add parameterized coverage for mismatched scope, snapshot, and preflight digests and their stable reason codes.
- P2 `src/medrec_research/cli.py:394` — Invalid status prevents fail-closed harness startup. Start in an unavailable state, serve fixed safe errors, and recover after a valid atomic replacement.
- P2 `src/medrec_research/harness.py:82` — Live last-known-good degradation path is untested. Add a same-server transition test from current status to corrupted input, degraded status, and disabled action context.

Do not treat this file as scientific authority, readiness evidence, or an implementation queue. Consult the current plan registry and implementation tests for active work.
